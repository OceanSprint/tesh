# A Markdown file with multiple shell sessions


```shell-session tesh-session="foo"
$ echo "foo"
foo
```


```shell-session tesh-session="foo"
$ echo "bar"
bar
```


```shell-session tesh-session="foo"
$ echo "foo with empty line"
foo

```
